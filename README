- Install VirtualBox
- Install Vagrant
- vagrant box add laravel/homested
- git clone https://github.com/laravel/homestead.git Homestead
- init.sh
- ssh-keygen -t rsa -C "you@homestead"
- Setup project at Homestead.yaml
- homestead.app -> homestead.test
- Add ip from Homestead.yaml to homestead.test and add it to /etc/hosts/hosts
- Clone this project

To create new project:
  - Install composer
  - composer create-project laravel/laravel --prefer-dist

Once all is good:
  - vagrant up
  - vagrant ssh
